{
  "name": "job-router",
  "version": "1.0.5",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.mjs",
  "scripts": {
    "test": "npx jest",
    "build": "tsup",
    "deploy": "npm run build && npm version patch && npm publish"
  },
  "tsup": {
    "entry": [
      "src/index.ts"
    ],
    "splitting": false,
    "sourcemap": true,
    "clean": true,
    "dts": true,
    "format": [
      "cjs",
      "esm"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.6.4",
    "ts-jest": "^29.1.1",
    "tsup": "^7.2.0"
  }
}
